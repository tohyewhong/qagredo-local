# Sample Input Data

This folder contains sample JSONL input files for quick local runs.

- `dev-data.jsonl`: default small sample dataset used by `scripts/run_online.sh`.

For real runs, place your own inputs under your chosen data path and set `run.input_file`.
